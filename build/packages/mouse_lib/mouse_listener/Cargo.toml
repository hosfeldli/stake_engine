[package]
name = "mouse_listener"
version = "0.1.0"
edition = "2021"

[dependencies]
rdev = "0.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "mouse_listener"
crate-type = ["cdylib"]  # This will produce a shared library

